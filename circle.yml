machine:
  node:
    version: 5.1.0

deployment:
  production:
    branch: master
    commands:
      - npm run postinstall
      - surge --project ./build --domain kasban.io

  staging:
    branch: feature/add-surge
    commands:
      - npm run postinstall
      - surge --project ./build --domain kasban-staging.surge.sh
