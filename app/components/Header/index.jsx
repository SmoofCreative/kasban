import React from 'react';

import './style.scss';

const Header = React.createClass({
  render() {
    return(
      <header className="header">
      Hello
      </header>
    );
  }
});

export default Header;
