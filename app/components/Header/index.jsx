import React from 'react';

import './style';

const Header = React.createClass({
  render() {
    return(
      <header className="header">
      Hello
      </header>
    );
  }
});

export default Header;
